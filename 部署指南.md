# 部署指南

把当前目录推到 GitHub，再在 Streamlit Cloud 起一个应用，最后把生成
的 URL 提交到作业即可。

## 本地先跑通

```powershell
python -m venv .venv
.venv\Scripts\activate
pip install -r requirements.txt
streamlit run app.py
```

浏览器会自动开 `http://localhost:8501`。流程过一遍：

1. 上传一张图片（猫、风景、玩具都行）。
2. 点 "Make my story!"。
3. 看：
   - 出现一句英文 caption；
   - 出现 50-100 词的故事；
   - 出现可播放的音频；
   - 下载按钮可以下载 `story.mp3`。

第一次运行会把 BLIP 和 flan-t5-base 的权重从 Hugging Face 下到本地
缓存（默认在 `~/.cache/huggingface`），属于一次性成本。

## 推到 GitHub

```powershell
git init
git add .
git commit -m "feat: storytelling app with HF pipelines"
git branch -M main
git remote add origin https://github.com/<your-user>/magic-story-maker.git
git push -u origin main
```

仓库需要设为 public（Streamlit Cloud 免费档要求），否则得在 Streamlit
Cloud 上装 GitHub App、授权访问私库。

## 在 Streamlit Cloud 部署

1. 用 GitHub 账号登录 <https://share.streamlit.io>。
2. 右上角 New app，选刚推上去的仓库和 `main` 分支。
3. Main file path 填 `app.py`。
4. 点 Advanced settings：
   - Python version 会自动读 `runtime.txt`（`python-3.11`）。
   - Secrets 留空。
5. 点 Deploy。

首次构建大约 3-5 分钟，日志会依次出现：

- `Cloning repository ...`
- `Installing dependencies ...`（pip 装 `requirements.txt`）
- `Starting up ...`（首次启动时下载模型权重）

构建完成后拿到形如 `https://<app-name>-<hash>.streamlit.app` 的 URL，
提交到作业即可。

## 常见问题

| 现象 | 原因 | 处理 |
| --- | --- | --- |
| 构建卡在 Installing dependencies | torch 安装慢 | 等 5-8 分钟，免费 CPU 节点首次确实慢 |
| 启动时 OOM (Killed) | 免费层内存装不下两个模型 | `STORY_MODEL` 改成 `google/flan-t5-small`，再次推送 |
| `gTTS` 报 Failed to connect | 出口网络限制 | 改用 `facebook/mms-tts-eng` 或本地 `pyttsx3` |
| 上传 PNG 报错 | PIL 模式问题 | `app.py` 已经走 `Image.open(...).convert("RGB")` |
| 中文图片标题不通顺 | BLIP base 仅支持英语 | 升级到 `Salesforce/blip-image-captioning-large` 或换中文 caption 模型 |

## 提交清单

仓库里需要的文件：

- `app.py`
- `requirements.txt`
- `runtime.txt`
- `README.md`
- `.streamlit/config.toml`
- `tests/test_app.py`
- `docs/设计说明.md`
- `docs/部署指南.md`

提交表单里需要填的：Streamlit Cloud 部署后拿到的 URL。
