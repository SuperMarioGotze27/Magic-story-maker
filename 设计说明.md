# 设计说明

## 整体流程

用户上传图片，BLIP 给出一句英文 caption，FLAN-T5 把 caption 扩写成
50-100 词的英文短故事，最后用 gTTS 合成 MP3，在页面上播放并提供下载。

## 模型

图像描述用 `Salesforce/blip-image-captioning-base`，作业指引明确推荐
这个模型。base 版约 990 MB，CPU 推理 1-3 秒一张，云端节点能跑得动。
为了不踩 transformers v5 即将移除的 `AutoModelForVision2Seq`，这里
显式用 `AutoModelForImageTextToText` + `AutoProcessor(use_fast=True)`
加载，然后再交给 `pipeline("image-to-text", ...)`。

故事生成用 `google/flan-t5-base`。最初尝试过 `gpt2`，即使 prompt 写
得再小心，仍偶尔输出成人化情节，比如 "his wife had died on Christmas
day"。换成 instruction-following 的 flan-t5-base 之后，叠加敏感词过
滤和兜底模板，结果稳得多。如果云端内存吃紧，可以把 `STORY_MODEL`
改成 `google/flan-t5-small`。

TTS 用 `gTTS`。优点是不用本地模型也不用音频驱动，直接走 Google 的
HTTP 接口拿到 MP3 字节流，刚好能塞进 Streamlit 的 `st.audio` 与下载
按钮。缺点是依赖外网；如果部署到屏蔽 Google 的环境，需要换成 HF 的
`facebook/mms-tts-eng` 或本地 `pyttsx3`。

## 代码组织

`app.py` 内函数按用途分组：

- `load_captioner` / `load_story_generator`：用 `@st.cache_resource`
  缓存 pipeline，避免每个 session 重复加载权重。
- `caption_image` / `generate_story` / `make_audio`：业务三段。
- `build_prompt` / `safe_template` / `is_acceptable` /
  `clean_story`：故事生成的内部工具。
- `main`：UI 层，全部逻辑直接写在里面（侧边栏、上传、按钮、结果展示）。

`generate_story` 的逻辑：先用 prompt + flan-t5 拿到原始输出，如果通过
`is_acceptable` 的安全门就走清洗流程，否则换成 `safe_template` 返回的
人工模板。最后统一交给 `clean_story` 做词数收口。

## 儿童内容防护

光靠 prompt 是不够的，所以做了几道防线：

1. 给 flan-t5 的 prompt 里塞了一段完整的 cat-on-sofa few-shot 示例，
   让它模仿口吻和正面结尾。
2. 采样参数取保守的 `temperature=0.8 / top_p=0.92 / top_k=50`，
   `repetition_penalty=1.4` + `no_repeat_ngram_size=3` 避免单词复读。
3. `UNSAFE_WORDS` 用前缀匹配覆盖 35+ 个伤害类词族，包括 `kill*`、
   `die*`、`hurt*`、`sick*`、`vet*`、`broken*` 等。
4. `is_acceptable` 在使用模型输出前做长度、黑名单和元叙事
   marker（`____`、URL、`@`、`chapter`、`I don't know` 等）三重检查。
5. 任意一项不合格就回退到 `safe_template`：人工撰写的小白兔分享浆果
   故事，长度天然落在窗口内。
6. `clean_story` 再做一次清洗：截掉模型尾部偶尔出现的 "If you like
   children's books, you'll love this story. Subscribe…" 这类元叙事
   片段；超过 100 词回退到最近句号；不足 50 词从 fillers 里选温馨句
   补足。

## 性能预期

| 阶段 | CPU 大致耗时 |
| --- | --- |
| 首次加载模型 | 30-60 秒（仅会话首次） |
| 图像描述 | 1-3 秒 |
| 故事生成 | 8-60 秒 |
| TTS 合成 | 1-2 秒 |

加上网络往返，单次端到端 6-13 秒是合理预期，不含模型首次下载。

## 测试与质量

- `tests/test_app.py` 覆盖 4 个测试类、22 个用例。所有依赖通过
  `types.ModuleType` 桩注入，不需要联网，运行时间 < 1 秒：
  ```bash
  python -m unittest discover -s tests -v
  ```
- 手动端到端：跑 `streamlit run app.py`，照 README 的 4 项手动检查
  做一遍，确认 caption / 词数 / 音频 / 下载都正常。
- 控制台 0 warning：通过 `USE_TF=0`、显式 `AutoModelForImageTextToText`
  + `use_fast=True`、以及把 Streamlit 的旧 `use_container_width` 改
  成新的 `width="stretch"`，把 transformers / Streamlit 的 future
  warning 全部消掉。

## 已知限制

- flan-t5-base 偶尔会写得平淡，但兜底模板和清洗能把内容拉回安全区。
  想要更生动的故事可以换 `flan-t5-large`（约 3 GB），云端内存压力会
  显著上升。
- gTTS 依赖 Google 网络可达。
- 黑名单是基于词形的，对内容安全要求更高的场景可以接 `unitary/toxic-bert`
  或类似分类器再过一次。
